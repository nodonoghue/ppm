package cli

func Parse(args []string) {
	//Brute force approach:
	//Check first char(s) are - or --
	//if - then look for a series of single char flags that match
	//if -- then look for longer flags which will need to be split int separate -- secions

}
